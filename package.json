{
  "name": "evoke-api-sdk",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "npx openapi-typescript openapi.json -o src/api-schema.ts",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "typescript": "^6.0.0"
  },
  "dependencies": {
    "openapi-fetch": "^0.17.0"
  }
}
